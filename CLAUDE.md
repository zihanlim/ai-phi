# AI-Phi Digital Agora

See `.claude/projects/ai-phi.md` for full project context.

## Quick Start
```bash
npm install
cp .env.example .env  # fill in DATABASE_URL + API keys
npm run db:push && npm run db:seed
npm run dev
```

## Before Any Commit
```bash
npm run lint && npm run build
```
